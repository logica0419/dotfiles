[project]
name = "dotfiles"
version = "0.1.0"
description = "Ansible playbooks for system configuration"
requires-python = ">=3.14.0, <3.15"
dependencies = [
    "ansible==13.4.0",
    "ansible-lint==26.2.0",
]
