# update role

- For scheduled automatic updates, refer to the Linux playbook (`update-linux.yaml`).
- For macOS updates, use `update-mac.yaml` and explicitly enable cask updates on the playbook side (disabled by default in the `update` role).
