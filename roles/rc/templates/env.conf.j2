# Shared environment variables for services and shell startup
# Format must remain compatible with both systemd EnvironmentFile and shell source.
PATH="{{ check_os_brew_base_path }}/bin:{{ check_os_brew_base_path }}/sbin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/go/bin:{{ ansible_facts.user_dir }}/go/bin:{{ ansible_facts.user_dir }}/.bun/bin:{{ ansible_facts.user_dir }}/.krew/bin:{{ ansible_facts.user_dir }}/.cargo/bin:{{ ansible_facts.user_dir }}/.local/bin:{{ ansible_facts.user_dir }}/.local/share/mise/shims"
