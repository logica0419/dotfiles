# completion role

- To enable Bash completion on macOS, prefer Homebrew `bash-completion@2` with `/opt/homebrew/etc/profile.d/bash_completion.sh` or `/usr/local/etc/profile.d/bash_completion.sh`.
- Skip tasks that generate completion files when the command is unavailable.
