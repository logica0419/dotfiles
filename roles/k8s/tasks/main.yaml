---
- name: Install Kubernetes components
  block:
    - name: Install Kubernetes packages
      community.general.homebrew:
        name:
          - kubectl
          - kind
          - krew
          - kustomize
          - helm
        state: present
    - name: Install kubectl plugins
      ansible.builtin.command: kubectl krew install oidc-login tree
      changed_when: false
